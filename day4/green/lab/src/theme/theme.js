import { createTheme } from '@mui/material/styles'

// TODO(Day4): discuss theme tokens: palette, typography, component defaults.
export const theme = createTheme({
  palette: { mode: 'light', primary: { main: '#00796b' } }
})